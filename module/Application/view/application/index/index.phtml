<div class="header header-filter" style="background-image: url('/img/bg-recep-ori.jpg');">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="brand">
                    <h1>Volley-ball.eu</h1>
                    <h3>Gestion de groupes, entrainements, matchs.</h3>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="main main-raised">
    <div class="section section-basic">
        <div class="container text-center">
            <div class="row">
                <h2>Recherche</h2>
                <h4>Recherchez un groupe ou un évènement en particulier.</h4>
                <div class="form-group label-floating is-empty text-left">
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">search</i>
                        </span>
                        <label class="control-label" for="main-search">
                            Nom de groupe, match, évènement
                        </label>
                        <input type="text" id="main-search" name="main-search" class="form-control" required="required" value="" aria-required="true">
                    </div>
                    <span class="material-input"></span>
                </div>
            </div>
        </div>
    </div>

    <?php if ($this->user->display != Application\Model\User::DISPLAY_TABLE): ?>
        <div class="section section-tabs">
            <div class="container">
                <div class="row text-center">
                    <h2>Vos évènements à venir</h2>
                    <div class="checkbox col-xs-12 text-center">
                        <?php foreach ($groups as $group): ?>
                            <label class="checkbox-inline">
                                <input type="checkbox" name="groups" class="group-filter" data-brand="<?= $group->brand ?>" checked="checked">
                                <?= $group->name ?>
                            </label>
                        <?php endforeach ?>
                    </div>
                    <?php if ($this->user->display == Application\Model\User::DISPLAY_LARGE): ?>
                        <div class="col-xs-12">
                            <?php foreach ($events as $event): ?>
                                <?php echo $this->partial('partial/events/extend.phtml', $event) ?>
                            <?php endforeach ?>
                        </div>
                    <?php else: ?>
                        <?php foreach ($events as $event): ?>
                            <?php echo $this->partial('partial/events/minimal.phtml', $event) ?>
                        <?php endforeach ?>
                    <?php endif ?>
                </div>
            </div>
        </div>
    <?php elseif ($this->user->display == Application\Model\User::DISPLAY_TABLE): ?>
        <div class="section section-basic">
            <div class="container">
                <div class="row text-center">
                    <div id='calendar'></div>
                </div>
            </div>
        </div>
    <?php endif ?>
</div>

<?php echo $this->partial('partial/signin.phtml') ?>
<?php echo $this->partial('partial/signup.phtml') ?>


